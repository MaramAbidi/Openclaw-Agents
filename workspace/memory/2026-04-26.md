# 2026-04-26

- Refreshed `test2/frontend/index.html` into an English homepage.
- Copied the latest Downloads video into `test2/frontend/media/hero-video.mp4` and used it as the hero background.
- Copied `Downloads/logo.png` into `test2/frontend/media/logo.png` and used it in the header and footer.
- Verified the local server serves `/`, `/media/logo.png`, and `/media/hero-video.mp4` successfully.
- Updated the homepage messaging so Lumiere Logistique is introduced first, with Lumicore x OpenClaw positioned as the personalized multi-agent AI system.
- Kept the cinematic logistics layout, gallery, and dashboard artwork while removing meta/editing language from the visible copy.
- Replaced the Lumicore dashboard image with a compact landscape graphic at `test2/frontend/images/lumicore-openclaw-card.svg` so it sits inside the existing dashboard card.
- Added `/login` and `/register` pages for Lumicore OpenClaw in `test2/frontend/`, with the same dark/orange theme and `lumicore-openclaw.png` artwork.
- Added MySQL-backed auth endpoints in `test2/index.js` and schema-aware user handling in `test2/db/auth.js`, including pending approval before login.
- The `users` table schema is `id`, `first_name`, `last_name`, `email`, `password_hash`, `role`, `status`, `approved_by`, `approved_at`, `created_at`, `updated_at`.
- Verified the auth flow against MySQL on `127.0.0.1:3307`: register writes a pending account, login returns `403` until `status` is changed to `approved`.
- `approved_by` is a foreign key to `users.id`, so admin approval should use `status='approved'` and optionally `approved_at`, not a free-text value.
