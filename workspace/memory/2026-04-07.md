# 2026-04-07

- OpenClaw gateway is healthy on `127.0.0.1:18789`; `openclaw status` reported `RPC probe: ok` and the gateway is listening loopback-only.
- WhatsApp is linked and the gateway health check reported recent auth age, so WhatsApp latency is not the main blocker.
- `dashboard-ui/src/App.jsx` uses a sequential fallback: `/v1/responses` -> `/v1/chat/completions` -> `/chat`.
- `test2/index.js` returns HTTP `502` from `/chat` whenever the OpenClaw CLI call times out or exits non-zero.
- `start.bat` launches the MCP server, the OpenClaw gateway, and the dashboard; the gateway/dashboard run on `18789`.
